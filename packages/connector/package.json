{
  "name": "@availworks/connector",
  "version": "0.0.1",
  "description": "Webview related API for VSCode extension.",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "devDependencies": {
    "@types/vscode": "^1.73.0"
  },
  "dependencies": {
    "ejs": "^3.1.3",
    "fs-extra": "^9.0.0"
  },
  "scripts": {
    "compile": "rm -rf lib && tsc -p ./tsconfig.json",
    "prepublishOnly": "npm run compile"
  },
  "publishConfig": {
    "access": "public"
  }
}
